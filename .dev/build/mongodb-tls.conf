net:
  bindIpAll: true
  tls:
    mode: requireTLS
    certificateKeyFile: /etc/certs/server.pem
    CAFile: /etc/certs/rootCA-cert.pem
    # Allow clients connection without client certificate, the collection is encrypted with TLS
    allowConnectionsWithoutCertificates: true
